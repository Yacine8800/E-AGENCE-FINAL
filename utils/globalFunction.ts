import { Bebas_Neue } from "next/font/google";
import type { Metadata } from "next";

export const bebas_beue = Bebas_Neue({
  weight: "400",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "E-AGENCE",
  description: "Votre agence digitale de confiance",
};
